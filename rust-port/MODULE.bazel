module(
    name = "libsqlite_rs",
    version = "0.1.0",
)

bazel_dep(name = "rules_rust", version = "0.68.1")
bazel_dep(name = "rules_cc", version = "0.0.17")
bazel_dep(name = "platforms", version = "0.0.10")

# 注: 内部子 crate (pager/btree/vdbe/util) 不需要在 MODULE.bazel 声明,
# 它们在同 repo 下, 用 package label (//src/util:util 等) 引用即可.
