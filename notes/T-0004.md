# T-0004 — port UTF-8 helpers

## 状态
queued

## 前置
- T-0002 完成

## 目标
src/util/utf8.rs:
- utf8_read(ptr) -> (char, byte_len)
- utf8_write(buf, c, n) -> byte_len
- utf8_char_count(s) -> 字符数

## 行为
- 1-4 字节 UTF-8
- surrogate pair 拒绝
- overlong 拒绝
- 5+ 字节拒绝

## 测试
12+ 用例: ASCII / 2/3/4-byte / 截断 / surrogate / overlong / 空串

## 经验
(完成后)
