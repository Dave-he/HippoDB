# T-0002 — port sqlite3_malloc family

## 状态
未开始(被 T-0001 占用, 等调度器)

## 前置
- T-0001 完成 (rust workspace + 8 FFI 函数 + 14 测试)

## 目标
替换 src/api.rs 中 sqlite3_malloc/malloc64/free 的桩为完整实现

## 实现要点
- 内部 API 在 src/util/alloc.rs (ZeroMalloc/Malloc/Realloc/Free)
- malloc(0) 返回非 null 可 free
- realloc 失败保留原指针
- free(NULL) no-op
- OOM 不 panic, 设置 db.malloc_failed
- 用 std::alloc::Global + 显式 Layout

## 测试要求
8 单元测试:
- size=0 返回非 null
- null free 是 no-op
- realloc 失败保留原指针
- 连续 alloc 后 free 全部
- double-free 检测(模拟)
- 对齐 8 字节
- OOM 标志读写
- 0 字节 realloc

## 经验
(Claude 完成后写在这里)
