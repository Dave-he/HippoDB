# T-0005 — port sqlite3_hash and GrowableArray

## 状态
queued

## 前置
- T-0002 完成

## 目标
src/util/hash.rs:
- 通用 hash table (linear probing)
- 支持字符串/整数键
- 对齐 sqlite3Hash
- GrowableArray<T> 动态数组, 容量满时倍增

## 测试
- insert/lookup/delete/resize 100k 元素压测
- GrowableArray 边界条件(空/满/倍增/收缩)

## 经验
(完成后)
